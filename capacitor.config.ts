import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taxproholygrail.app',
  appName: 'Tax Pro Holy Grail',
  webDir: 'dist'
};

export default config;
